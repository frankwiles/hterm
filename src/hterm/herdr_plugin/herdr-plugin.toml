id = "hterm.lifecycle"
name = "hterm Lifecycle"
version = "0.1.0"
min_herdr_version = "0.8.0"
description = "Run hterm project post-hooks when Herdr workspaces close"
platforms = ["macos"]

[[events]]
on = "workspace.closed"
command = ["/bin/sh", "workspace-closed.sh"]
